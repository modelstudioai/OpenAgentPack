{
	"name": "@openagentpack/playbooks",
	"version": "0.0.1",
	"private": true,
	"license": "Apache-2.0",
	"description": "OpenAgentPack shared playbook protocol, seed catalog, and runtime resolver",
	"type": "module",
	"exports": {
		".": {
			"bun": "./src/index.ts",
			"default": "./src/index.ts"
		}
	},
	"scripts": {
		"test": "bun test",
		"typecheck": "tsc --noEmit"
	},
	"devDependencies": {
		"@types/bun": "^1.3.14",
		"typescript": "^7.0.2"
	}
}
